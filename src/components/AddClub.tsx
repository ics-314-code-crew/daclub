import { Card, ListGroup } from "react-bootstrap"
import { Link } from "react-bootstrap-icons"

const AddClub = ({}) => {
    <Card className="h-100 shadow-sm border-0" style={{ borderRadius: '10px', minHeight: '350px' }}>
    
  </Card>
}